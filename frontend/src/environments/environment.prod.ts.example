export const environment = {
  production: true,
  baseURL: '/api', // Adjust the port if necessary
  auth: {
    domain: <YOUR AUTH0 DOMAIN>,
    clientId: <YOUR AUTH0 CLIENT ID>,
    audience: <YOUR AUTH0 AUDIENCE>,
    redirect_uri: window.location.origin,
    errorPath: '/error',
  },
};
